<section>
    <h1>Products - Handlebars</h1>
    <div class="products-container">
        {{#each products}}
        <div class="product-card">
            <span>{{this.title}}</span>
            <p>${{this.price}}</p>
        </div>
        {{/each}}
    </div>
</section>